import { StyleSheet } from "react-native";

const defaultStyles = StyleSheet.create({
    container: {
        backgroundColor: '#001233',
        flex: 1,
    },
    text: {
        color: 'white'
    }
})

export {
    defaultStyles
}